[tool.poetry]
name = "k-nearest-neighbors-classification"
version = "1.0.0"
description = "🏵🏵🏵 Pythonでk近傍法を実装してみる！"
authors = ["osawa-koki <koki@osawa.dev>"]
license = "Apache-2.0"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
